# mobile/android/ (not started)

Foreground-only node (same constraint as iOS — see `../../docs/architecture.md`). May later use a short-lived foreground service to extend the sync window slightly, but does not rely on always-on background execution.
